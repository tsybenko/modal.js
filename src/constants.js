const BEFORE_OPEN = 'beforeOpen';
const ON_OPEN = 'onOpen';
const OPENED = 'opened';

const BEFORE_CLOSE = 'beforeClose';
const ON_CLOSE = 'onClose';
const CLOSED = 'closed';

const MODE_DEV = 'development';
const MODE_PROD = 'production';


module.exports = {
	BEFORE_OPEN,
	ON_OPEN,
	OPENED,
	BEFORE_CLOSE,
	ON_CLOSE,
	CLOSED,
	MODE_DEV,
	MODE_PROD
};